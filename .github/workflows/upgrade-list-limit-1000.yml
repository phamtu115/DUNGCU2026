name: Upgrade list limit 1000

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/upgrade-list-limit-1000.yml'
      - 'scripts/upgrade-list-limit-1000.mjs'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  upgrade:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Apply 1000 row limits
        run: node scripts/upgrade-list-limit-1000.mjs
      - name: Verify build and tests
        run: npm run check
      - name: Commit runtime update
        run: |
          if git diff --quiet -- src/app.js; then
            echo "No runtime changes required"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/app.js
          git commit -m "Increase booking stay payment lists to 1000 rows"
          git push
